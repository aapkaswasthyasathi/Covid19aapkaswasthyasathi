<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.indevelopment">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_stetho"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_stetho_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VisitCount"></activity>
        <activity android:name=".Viewweb" />
        <activity android:name=".Official" />
        <activity android:name=".CovidQuestions" />
        <activity android:name=".Finalpage" />
        <activity
            android:name=".Questions"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".SignIn"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>